paths:
  mystream_0:
  mystream_1:
  mystream_2:

    #    runOnDemand: ffmpeg -f v4l2 -i /dev/video2 -preset ultrafast -b:v 600k -c copy -vcodec h264 -f rtsp rtsp://localhost:8554/mystream
    # runOnDemandRestart: yes
